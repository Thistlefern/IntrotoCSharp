using System;

namespace IfStatementsExercise
{
    class Program
    { // Review Questions
      // 1. An if statement is a conditional statement in which one or more pieces of criteria are checked to be true, before enacting code that corresponds to whether the criteria was true or otherwise.
      // 2. If the condition is true in an if statement, then the corresponding code is initiated.
      // 3. An else statement takes a previous if statement, and gives the code to initiate in the case that the if statement is false.
      // 4. An else if statement gives additional criteria to a previous if statement, but prior to a final else statement.
      // 5. 
      // 6. 
      // 7. 
      // 8. 
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
